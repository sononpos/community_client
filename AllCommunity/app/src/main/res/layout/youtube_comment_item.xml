<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="com.sononpos.allcommunity.youtube.CommentInfo" />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:clipToPadding="false"
        android:orientation="horizontal"
        style="@style/AppTheme.ArticleItemBase"
        >
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            >
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:text="@{item.sComment}"
                tools:text="Title Title Title Title Title"
                style="@style/AppTheme.TextView_Youtube"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="@{item.sAuthName}"
                tools:text="Title Title Title Title Title"
                style="@style/AppTheme.TextView_Youtube.Small"
                />
        </LinearLayout>
    </LinearLayout>
</layout>